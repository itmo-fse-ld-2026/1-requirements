\usepackage{fontspec}
\usepackage[english, russian]{babel}
\usepackage{graphicx}
\usepackage[a4paper,
            left=3cm,
            right=3cm,
            top=2cm,
            bottom=3cm]{geometry}

\setmainfont{CMU Serif}
\setsansfont{CMU Sans Serif}
\setmonofont{CMU Typewriter Text}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,        % цветные ссылки (false - черные рамки)
    linkcolor=black,        % цвет ссылок на разделы
    urlcolor=blue,          % цвет URL-ссылок
    citecolor=green,        % цвет ссылок на литературу
    pdfborder={0 0 0}       % убрать рамки вокруг ссылок
}

\usepackage{titlesec}

\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{4}
\newcommand{\subsubsubsubsection}[1]{\paragraph{#1}\mbox{}\\}

\usepackage{tabularx}
\usepackage{float} % for [H] option in table environment

\newenvironment{compactitem}{%
  \setlength{\leftmargini}{8mm}%
  \vspace{-0.5\topsep}%
  \begin{itemize}%
    \setlength{\itemsep}{3pt}%
    \setlength{\parskip}{0pt}%
}{%
  \end{itemize}%
  \vspace{-\topsep}%
  \ignorespacesafterend%
}

\usepackage{tabularray}
  \UseTblrLibrary{varwidth}

\DefTblrTemplate{firsthead,middlehead,lasthead}{default}{
}
\DefTblrTemplate{firstfoot}{default}{
  \UseTblrTemplate{contfoot}{default}
}
\DefTblrTemplate{middlefoot}{default}{
  \UseTblrTemplate{contfoot}{default}
  \UseTblrTemplate{capcont}{default}
}
\DefTblrTemplate{lastfoot}{default}{
  \UseTblrTemplate{note}{default}
  \UseTblrTemplate{remark}{default}
  \addvspace{9pt}
  \UseTblrTemplate{caption}{default}
}
\DeclareTblrTemplate{contfoot-text}{default}{См. далее}

\NewTblrEnviron{reqtable}
\SetTblrInner[reqtable]{
  rowhead = 1, hlines, vlines,
  row{1} = {font=\bfseries, halign=c},
  rowsep = 4pt,
  measure=vbox,
}
\SetTblrOuter[reqtable]{
  postsep = 9pt,
  long,
}